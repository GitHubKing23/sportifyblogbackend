// Jest setup for the project.
// MongoDB-based test setup has been removed because the project now uses ArangoDB.
// If you need to run tests that depend on a database, either:
//  - Configure an ArangoDB test instance and update this file to connect to it, or
//  - Mock database calls in tests.

console.log('🧪 jest.setup.js: MongoDB setup removed. Ensure tests are updated to use Arango or mocks.');
jest.setTimeout(20000);
