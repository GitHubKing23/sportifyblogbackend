// Tests disabled: project no longer uses MongoDB/Mongoose.
// Rewrite controller tests to mock `services/blogServiceArango` or use an Arango test DB.
describe('blogControllers tests disabled', () => {
  test('placeholder', () => expect(true).toBe(true));
});
